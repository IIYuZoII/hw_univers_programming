#include "entity.h"


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>


void printer_scanner(struct mail *array, int k, int count);


void printer (struct mail *array, int count);